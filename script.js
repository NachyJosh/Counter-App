const num = document.getElementById('num')

console.log(a)

const increase = document.getElementById('plus');

increase.onc

function inc(){
    let result = parseInt(num.textContent) + 1;

    console.log(result);

    num.textContent = result;
}